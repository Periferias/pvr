apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pvr.fullname" . }}-test-connection"
  labels:
    {{- include "pvr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pvr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
