apiVersion: v1
kind: Service
metadata:
  name: {{ include "pvr.fullname" . }}
  labels:
    {{- include "pvr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pvr.selectorLabels" . | nindent 4 }}
